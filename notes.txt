# function/class guidelines:
# 1. Short and concise
# 2. Specify return type
# 3. Make as simple and reusable as possible
# 4. Document all your functions
# 5. Handle errors appropriately
# system requirement:
# 6. Establish Reorder Point (if quantity < #, send message “hey, we’re running out of this!”

# Extensions/Packages: icecream, PySide6 - venv (virtual environment)

#GitHub

# PySide6 - Qt Designer: Main Widget -> QTabWidget -> QTableView
# When working with QTabWidget and QTableView here are the functions that is neccesary to ineract with the QTabWidget 
# and QTableView to display information in a table from JSON file.
# 
# Create a function to load the json data.
# Create a function to monitor the json data, if it changes/updates, reload the json data and repopulate the table.
# Create a custom model to manage how the data will be display in the table using class Name(QAbstractTableModel)
    # Create the row and column count (either set a fixed number or using len() to dynamically change the row/column count).
    # Create a def data() function to control how each cell will display information.
    # Create a def headerData() to set the header of the table: | Item Name | quantity | location | .....
# Create a custom function inside main widget class to populate the tab of QTabWidget and QTableView.